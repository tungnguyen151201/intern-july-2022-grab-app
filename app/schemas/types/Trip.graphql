type Trip {
    id: ID!
    customer: User!
    driver: User
    departure: String!
    destination: String!
    createAt: String!
    startTime: String
    endTime: String
    totalPaid: Float
    status: TripStatus!
}