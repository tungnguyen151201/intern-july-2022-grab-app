type Query {
  """
  Get current user information
  """
  me: User
  getUsers(criteria: UserSearchInput): [User]
  myTrips(criteria: MyTripSearchInput): [Trip]
  getTrips(criteria: TripSearchInput): [Trip]
  getPendingTrips: [Trip]
}
